import { SortPostsPipe } from './sort-posts-pipe';

describe('SortPostsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPostsPipe();
    expect(pipe).toBeTruthy();
  });
});
