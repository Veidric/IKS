import { FormatDateProfilePipe } from './format-date-profile-pipe';

describe('FormatDateProfilePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateProfilePipe();
    expect(pipe).toBeTruthy();
  });
});
